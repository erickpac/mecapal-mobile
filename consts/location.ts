/**
 * The country we operate in by default. When Mekapal expands to multiple
 * countries, this should be replaced by the user's selected country
 * (likely stored on the User entity at registration time).
 */
export const DEFAULT_COUNTRY_CODE = 'GT';
